# Solana Indexer Examples

This directory contains a set of examples that demonstrate how to use the `solana-indexer-sdk`.

To run an example, use the following command:

```bash
cargo run --example <example_name>
```
